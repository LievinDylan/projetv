<%- include('partials/header') %>
<link rel="stylesheet" href="/css/mode.css">
</head>
<%-include('partials/nav') %>
<body>
    <div class="container">
        <h1 class="title">Voici votre prochaine mission</h1>
        <div class="section">
            <div class="section-left">
                <img class="section-img" src="/img/img_agent/<%= agent.name %>.png" alt="Une image de <%= agent.name %>">
                <em>"<%= agent.quote %>"</em>  
            </div>
            <div class="section-right">
                <% if (agent.name === "Kayo") { %>
                    <h1>KAY/O</h2>
                <% } else { %>
                    <h1><%= agent.name %></h1>
                <% } %>
                        <p><%= agent.role.name %></p>
                    <h3>Arsenal disponible :</h3>
                        <p><span><%= agent.weapons[k].type.name %></span> : <%= agent.weapons[k].name %></p>
                        <p><span><%= agent.weapons[l].type.name %></span> : <%= agent.weapons[l].name %></p>
                        <p><span><%= agent.weapons[m].type.name %></span> : <%= agent.weapons[m].name %></p>
                         <% if (k === 0 || l === 0 || m === 0) { %>
                             <em class="classic">Le classic... Une vraie mission !</em>
                         <% } %>
                    <h3>Compétence interdite :</h3>
                        <% const agentSkills = agent.skills.map(skill => skill) %>
                        <% for( let i = 0; i < agentSkills.length; i++ ) { %>
                            <% if (i === n) { %>
                                <p><%= agentSkills[n].name %></p>
                            <% } %>
                        <% } %>
                        <a href="/generator/hard" class="chance"><button>Nouvelle mission ?</button></a>
            </div>
        </div>

    </div>
</body>
</html>