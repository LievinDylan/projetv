<%- include('partials/header') %>
<link rel="stylesheet" href="/css/mode.css">
</head>
<%-include('partials/nav') %>
<body>
    <div class="container">
        <h1 class="title">Voici votre prochaine mission</h1>
        <div class="section">
            <div class="section-left">
                <img class="section-img" src="/img/img_agent/<%= agent.name %>.png" alt="Une image de <%= agent.name %>">
                <em>"<%= agent.quote %>"</em>  
            </div>
            <div class="section-right">
                <% if (agent.name === "Kayo") { %>
                    <h1>KAY/O</h2>
                <% } else { %>
                    <h1><%= agent.name %></h1>
                <% } %>
                <p><%= agent.role.name %></p>
                <h3>Arsenal :</h3>
                <p>Illimité, et selon vos goûts</p>
                <h3>Compétences :</h3>
                <p>Vous voilà en pleine forme et libre d'utiliser toutes vos compétences quand elles sont disponibles !</p>
                <a href="/generator/easy" class="chance"><button>Nouvelle mission ?</button></a>
            </div>
        </div>

    </div>

</body>
</html>